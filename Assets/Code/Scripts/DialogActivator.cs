using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DialogActivator : MonoBehaviour
{
    /*public bool canActivate = false;
    public string[] lines;
    public Sprite theNpcSprite; //img caja de dialogo hacer array como texto

   
    void Update()
    { //activeInhierarchy si esta activo en la jerarquia
        if (canActivate && Input.GetKeyDown(KeyCode.F) && !DialogueManager.instance.dialogBox.activateInHierarchy)
        {
            DialogueManager.instance.ShowDialog(lines, theNpcSprite);
        }
    }

    private void OnTriggerEnter2D(Collider2D collision)
    {
        if (collision.CompareTag("Player"))
        {
            canActivate = true;
        }
    }

    private void OnTriggerExit2D(Collider2D collision)
    {
        if (collision.CompareTag("Player"))
        {
            canActivate = false;
        }
    }*/
}
