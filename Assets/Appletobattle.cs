using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Appletobattle : MonoBehaviour
{

    /*private Pickableobjct _pcoRef;
    private CombatOnTrigger _bsRef;

    // Start is called before the first frame update
    void Start()
    {
        _pcoRef = GameObject.Find("manzana").GetComponent<Pickableobjct>();
        _bsRef = GameObject.Find("PIEDRUSCO (7)").GetComponent<CombatOnTrigger>();
    }


   
    private void OnTriggerStay2D(Collider2D collision)
    {
        if (_pcoRef.PickedUp == true && Input.GetKeyDown(KeyCode.E))
        {

            _bsRef.enabled = true;
        }
        else
        {
            _bsRef.enabled = false;
        }
    }*/
}
